TrajectoryPlannerROS:

  # Robot Configuration Parameters
  acc_lim_x: 10.0
  acc_lim_theta:  20.0

  max_vel_x: 0.5
  min_vel_x: 0.18

  max_vel_theta: 1.57
  min_vel_theta: -1.57
  min_in_place_vel_theta: 0.20

  holonomic_robot: false
  escape_vel: -0.5

  # Goal Tolerance Parameters
  yaw_goal_tolerance: 0.60
  xy_goal_tolerance: 0.45
  latch_xy_goal_tolerance: true

  # Forward Simulation Parameters
  sim_time: 2.5
  sim_granularity: 0.02
  angular_sim_granularity: 0.02
  vx_samples: 10
  vtheta_samples: 36
  controller_frequency: 20.0

  # Trajectory scoring parameters
  meter_scoring: true # Whether the gdist_scale and pdist_scale parameters should assume that goal_distance and path_distance are expressed in units of meters or cells. Cells are assumed by default (false).
  occdist_scale:  0.35 # Increase obstacle cost so the local planner prefers going around clutter instead of squeezing straight ahead.
  pdist_scale: 0.55  # Reduce path clinging so the robot can deviate around temporary obstacles.
  gdist_scale: 0.90 # Keep making forward progress toward the local goal after detouring.

  heading_lookahead: 0.45  #How far to look ahead in meters when scoring different in-place-rotation trajectories
  heading_scoring: false  #Whether to score based on the robot's heading to the path or its distance from the path. default false
  heading_scoring_timestep: 0.8   #How far to look ahead in time in seconds along the simulated trajectory when using heading scoring (double, default: 0.8)
  dwa: true #Whether to use the Dynamic Window Approach (DWA)_ or whether to use Trajectory Rollout
  simple_attractor: false
  publish_cost_grid_pc: true

  #Oscillation Prevention Parameters
  oscillation_reset_dist: 0.15 # Require clearer movement before reset so obstacle avoidance does not jitter in place.
  escape_reset_dist: 0.2
  escape_reset_theta: 0.2
